import React from "react";
import ReactDOM from "react-dom/client";
import InterAlia from "./App.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <InterAlia />
  </React.StrictMode>
);
